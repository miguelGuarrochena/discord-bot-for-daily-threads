# Daily Threads Discord Bot

A **Node.js** Discord bot that automatically creates a daily thread for artists to log and track their task progress.  
Keeps the team organized and ensures daily updates are recorded efficiently.

**Setup:**  
1. Clone the repo  
2. Install dependencies: `npm install`  
3. Add your bot token and channel ID in a `.env` file  
4. Run: `node index.js`
